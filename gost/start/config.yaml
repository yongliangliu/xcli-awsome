name: start
description: Start the UDP-enabled mixed proxy and enable launch-at-login
type: cli
params:
  - name: port
    type: number
    description: Listen port (default 10808)
    placeholder: "10808"
command: 'bash "$HOME/.xcli/gost/gost-ctl.sh" start {{port_flag}}'
passThrough: true
